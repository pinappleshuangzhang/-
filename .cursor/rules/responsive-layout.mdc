---
description: 响应式布局与测试规范（边距、版心、测试分辨率）
alwaysApply: true
---

# 响应式布局规范

## 布局基线

- 页面四周固定边距：`30px`，**所有分辨率下恒定不变**（上下左右均为 30px，不随屏幕增大）。
- 边缘锚定的 UI（导航、语言切换、滚动提示、页角信息等）始终贴 30px 边距定位。
- 推荐容器写法：`width: calc(100% - 60px)`，居中；上下用 `top: 30px` / `bottom: 30px`。
- 正文内容块（段落、卡片栅格）如需限宽提升可读性，可另行设置 `max-width` 并居中，但不影响边缘 UI 的 30px 定位。
- 全屏媒体（背景图、视频）边到边铺满，不受 30px 边距约束。

## 测试分辨率

- 桌面端：1280×800、1440×900、1920×1080、2560×1440、3440×1440、3840×2160
- 平板：768×1024
- 移动端：375×812、390×844、412×915

当前阶段只做桌面端适配；平板与移动端布局等设计稿提供后再实现，不要自行推断。

## 动画响应式规则

- 避免固定像素动画距离，优先百分比、视口单位、`clamp()`。
- 大屏使用 `gsap.matchMedia()` 调整 GSAP 时间轴。
- 在不同视口高度下检查 ScrollTrigger 的 pin 时长与触发点。
